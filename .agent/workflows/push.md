---
description: อัปเดตงานทั้งหมดและ push ขึ้น GitHub ในปุ่มเดียว
---

// turbo-all
1. ตรวจสอบสถานะและอัพโหลดไฟล์
```powershell
git add . ; git commit -m "update project: fixed date formatting and thai language settings" ; git push
```
