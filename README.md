# โครงการติดตามงานก่อสร้าง - อบต.สงยาง (Modular Version)

## 📋 สิ่งที่ปรับปรุงใหม่

### 1. แยกไฟล์ (Modularization)
โครงสร้างไฟล์ใหม่:
```
songyangproject/
├── index_modular.html    # ไฟล์หลัก (ใช้แทน index.html เดิม)
├── css.html              # ไฟล์ CSS แยก
├── js_main.html          # JavaScript หลัก (CRUD + Drive Upload)
├── js_utils.html         # JavaScript Utilities (UI + Date Functions)
└── Code.gs               # Google Apps Script Backend
```

### 2. ปรับปรุงการส่งรูปภาพ
- **เดิม**: ส่ง Base64 ขนาดใหญ่ไปยัง AI โดยตรง → ทำให้ timeout
- **ใหม่**: อัปโหลดรูปไปยัง Google Drive ก่อน → ส่งลิงก์ให้ AI วิเคราะห์ → เสถียรกว่ามาก

## 🚀 วิธีการติดตั้ง

### ขั้นตอนที่ 1: เตรียม Google Drive Folder
1. เปิด Google Drive
2. สร้างโฟลเดอร์ใหม่ชื่อ "Project Images" (หรือชื่ออื่นตามต้องการ)
3. คลิกขวาที่โฟลเดอร์ → "Get link" → คัดลอก ID จาก URL
   - ตอนนี้ URL จะเป็น: `https://drive.google.com/drive/folders/FOLDER_ID_HERE`
   - คัดลอก `FOLDER_ID_HERE` ไว้

### ขั้นตอนที่ 2: เตรียม Gemini API Key
1. ไปที่ https://aistudio.google.com/app/apikey
2. สร้าง API Key ใหม่
3. คัดลอก API Key ไว้

### ขั้นตอนที่ 3: อัปโหลดไฟล์ไปยัง Google Apps Script
1. เปิด Google Apps Script: https://script.google.com
2. สร้างโปรเจกต์ใหม่หรือเปิดโปรเจกต์เดิม
3. **อัปโหลดไฟล์ทั้งหมด**:
   - `Code.gs` → แทนที่ไฟล์ `Code.gs` เดิม
   - `index_modular.html` → เปลี่ยนชื่อเป็น `index.html` (หรือสร้างใหม่)
   - `css.html` → สร้างไฟล์ HTML ใหม่
   - `js_main.html` → สร้างไฟล์ HTML ใหม่
   - `js_utils.html` → สร้างไฟล์ HTML ใหม่

### ขั้นตอนที่ 4: แก้ไข Code.gs
เปิดไฟล์ `Code.gs` และแก้ไขค่าต่อไปนี้:

```javascript
const SHEET_ID = 'YOUR_SHEET_ID_HERE'; // แทนที่ด้วย Google Sheet ID ของคุณ
const DRIVE_FOLDER_ID = 'YOUR_DRIVE_FOLDER_ID_HERE'; // แทนที่ด้วย Folder ID จากขั้นตอนที่ 1

// ในฟังก์ชัน parseImageFromDrive
const apiKey = 'YOUR_GEMINI_API_KEY_HERE'; // แทนที่ด้วย Gemini API Key จากขั้นตอนที่ 2
```

### ขั้นตอนที่ 5: Deploy
1. คลิก "Deploy" → "New deployment"
2. เลือก type: "Web app"
3. ตั้งค่า:
   - Execute as: **Me**
   - Who has access: **Anyone**
4. คลิก "Deploy"
5. คัดลอก URL ที่ได้

### ขั้นตอนที่ 6: อัปเดต API_URL
เปิดไฟล์ `js_main.html` และแก้ไข:

```javascript
const API_URL = 'YOUR_DEPLOYED_URL_HERE'; // แทนที่ด้วย URL จากขั้นตอนที่ 5
```

## 📖 วิธีใช้งาน

### การอัปโหลดรูปเพื่อกรอกข้อมูลอัตโนมัติ
1. คลิกปุ่ม "+" (FAB) เพื่อเพิ่มโครงการใหม่
2. คลิกปุ่ม "อัปโหลดรูปเพื่อกรอกข้อมูลอัตโนมัติ (AI)"
3. เลือกรูปภาพเอกสารโครงการ
4. ระบบจะ:
   - อัปโหลดรูปไปยัง Google Drive
   - ส่งลิงก์ให้ Gemini AI วิเคราะห์
   - กรอกข้อมูลอัตโนมัติลงในฟอร์ม
5. ตรวจสอบและแก้ไขข้อมูลตามต้องการ
6. คลิก "บันทึกข้อมูล"

## 🔧 ข้อดีของระบบใหม่

### 1. โครงสร้างโค้ดดีขึ้น
- **อ่านง่าย**: แยกไฟล์ตามหน้าที่ (CSS, JS Main, JS Utils)
- **แก้ไขง่าย**: แก้ไขเฉพาะส่วนที่ต้องการโดยไม่กระทบส่วนอื่น
- **ขยายง่าย**: เพิ่มฟีเจอร์ใหม่ได้โดยไม่ต้องแก้ไขไฟล์ใหญ่

### 2. การอัปโหลดรูปเสถียรกว่า
- **ไม่ timeout**: ไม่ส่ง Base64 ขนาดใหญ่ผ่าน HTTP
- **จัดการง่าย**: รูปทั้งหมดเก็บใน Google Drive
- **ปลอดภัย**: ควบคุมสิทธิ์การเข้าถึงได้

### 3. ประสิทธิภาพดีขึ้น
- **โหลดเร็ว**: แยกโหลดไฟล์ตามความจำเป็น
- **Cache ได้**: Browser สามารถ cache ไฟล์แยกได้

## 📝 หมายเหตุ

### สำหรับ Google Apps Script
- ใช้ `<?!= include('filename'); ?>` เพื่อดึงไฟล์ HTML อื่นเข้ามา
- ไฟล์ที่ include ต้องอยู่ในโปรเจกต์เดียวกัน
- ไม่ต้องใส่นามสกุล `.html` ใน include()

### การ Debug
- เปิด Console (F12) เพื่อดู error
- ตรวจสอบว่า API_URL ถูกต้อง
- ตรวจสอบว่า SHEET_ID, DRIVE_FOLDER_ID, และ API_KEY ถูกต้อง

## 🆘 แก้ปัญหา

### ปัญหา: "Upload failed"
- ตรวจสอบ DRIVE_FOLDER_ID ว่าถูกต้อง
- ตรวจสอบว่า Apps Script มีสิทธิ์เข้าถึง Drive

### ปัญหา: "AI parsing failed"
- ตรวจสอบ Gemini API Key ว่าถูกต้อง
- ตรวจสอบว่ารูปภาพชัดเจนและอ่านได้

### ปัญหา: "include is not defined"
- ตรวจสอบว่าอัปโหลดไฟล์ครบทุกไฟล์
- ตรวจสอบชื่อไฟล์ว่าตรงกับที่ include()

## 📞 ติดต่อ
หากมีปัญหาหรือข้อสงสัย สามารถติดต่อผู้พัฒนาได้ที่:
- Email: [your-email@example.com]
- GitHub: [your-github-username]
